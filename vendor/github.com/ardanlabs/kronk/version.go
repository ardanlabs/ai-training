package kronk

// Don't forget to review any example code when updating the version.

const currentVersion = "0.9.1"

// Version returns the current version of the kronk package.
func Version() string {
	return currentVersion
}
