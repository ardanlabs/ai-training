# Chat App

A simple React chat interface that connects to a local chat API endpoint.

## Installation

```bash
npm install
```

## Development

```bash
npm run dev
```

## Build

```bash
npm run build
```

The static files will be generated in the `dist` folder.

## Usage

Make sure your chat API is running on `http://localhost:3000` before using the app.
